# Enigma OS live session: auto-login the live user straight into Plasma
# (Wayland). On an installed system this file is not present, so SDDM shows
# the normal login screen.
[Autologin]
User=enigma
Session=plasma
Relogin=false
